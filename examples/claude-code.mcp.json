{
  "_comment": "Claude Code MCP config for Thalamus. Copy this to a file named .mcp.json at the root of YOUR project, then edit the values marked FILL-IN below. Claude Code expands ${VAR} and ${VAR:-default}, so if you already exported the THALAMUS_NEO4J_* vars from the Quickstart, the defaults here just work for a local setup.",
  "mcpServers": {
    "thalamus": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run", "python", "-m", "thalamus.cli", "serve",
        "--repo", "${CLAUDE_PROJECT_DIR:-.}",
        "--repo-id", "${THALAMUS_REPO_ID:-my-project}",
        "--tenant", "${THALAMUS_TENANT:-local}",
        "--max-structural-items", "12",
        "--max-memory-chars", "1000"
      ],
      "env": {
        "THALAMUS_NEO4J_URI": "${THALAMUS_NEO4J_URI:-bolt://localhost:7687}",
        "THALAMUS_NEO4J_USER": "${THALAMUS_NEO4J_USER:-neo4j}",
        "THALAMUS_NEO4J_PASSWORD": "${THALAMUS_NEO4J_PASSWORD:-thalamuspw}",
        "HF_HUB_OFFLINE": "${HF_HUB_OFFLINE:-1}"
      }
    }
  }
}
