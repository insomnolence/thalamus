[project]
name = "thalamus-core"
version = "0.1.0"
description = "Shared contracts: protocols, types, IDs, exceptions (dependency-free)."
license = "Apache-2.0"
authors = [{ name = "The Thalamus Authors" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/thalamus"]
